# GitHub App credentials for API access
github:
  app_id: 245286
  installation_id: 59973090

sources:
  # All repos under these orgs are included automatically
  orgs:
    - name: conforma
    - name: enterprise-contract

  # Additional repos outside the orgs above
  repos:
    - konflux-ci/build-definitions
    - konflux-ci/build-trusted-artifacts
    - konflux-ci/docs
    - konflux-ci/e2e-tests
    - konflux-ci/konflux-ci.github.io
    - konflux-ci/konflux-test
    - konflux-ci/release-service-catalog
    - konflux-ci/tektor
    - redhat-appstudio/infra-deployments
    - redhat-appstudio/tssc-dev-multi-ci
    - redhat-appstudio/tssc-sample-templates
    - tektoncd/chains
    - release-engineering/rhtap-ec-policy

# Reviewer leaderboard settings
leaderboard:
  # Data horizon: how many days of review history to aggregate. This is the
  # widest range the leaderboard can show; the dashboard's interval selector
  # narrows it further client-side. Defaults to 90 if omitted.
  window_days: 90

# Team members — PRs from these authors are always shown,
# even in repos outside the orgs above
authors:
  - Acepresso
  - cuipinghuo
  - dheerajodha
  - joejstuart
  - robnester-rh
  - simonbaird
  - st3penta
  - jsmid1
  - BohdanMar
